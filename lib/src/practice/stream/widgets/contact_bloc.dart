import 'dart:async';

import 'package:portfolio/src/model/contact.dart';
import 'package:portfolio/src/service/contact_service.dart';

class ContactBloc {
  // final StreamController<int> _contactCounter = StreamController<int>();
  // Stream<int> get contactCounter => _contactCounter.stream;
//
  // Stream<List<Contact>> get contactListView async* {
  //   yield* await ContactServeice.browse();
  // }
//
  // ContactBloc() {
  //   contactListView.listen((list) => _contactCounter.add(list.length));
  // }
}
